# 로컬 개발 환경 설정
# .env 파일 (실제 운영시에는 값들을 채워넣으세요)

# ===================================
# 포트 설정
# ===================================
BACKEND_PORT=8001
FRONTEND_PORT=3000
DB_PORT=5433

# ===================================
# 데이터베이스 설정
# ===================================
POSTGRES_DB=capstone_db
POSTGRES_USER=capstone_user
POSTGRES_PASSWORD=capstone_password

# 개별 DB 설정 (로컬 개발용)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=capstone_db
DB_USER=capstone_user
DB_PASSWORD=capstone_password
DB_HOST=db
DB_PORT=5432
DB_CONN_MAX_AGE=600

# 클라우드 DB 설정 (프로덕션용)
# DATABASE_URL=postgresql://user:password@host:port/database

# ===================================
# Django 설정
# ===================================
DEBUG=True
DJANGO_SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1

# ===================================
# Next.js 설정
# ===================================
NEXT_PUBLIC_API_URL=http://localhost:8001
NODE_ENV=development

# ===================================
# AWS 설정 (클라우드 배포용)
# ===================================
# 로컬 개발시에는 비워두고, 실제 배포시에만 설정
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=ap-northeast-2
AWS_STORAGE_BUCKET_NAME=
AWS_S3_CUSTOM_DOMAIN=

# AWS Bedrock 설정
AWS_BEDROCK_REGION=us-east-1
AWS_BEDROCK_MODEL_ID=anthropic.claude-v2

# ===================================
# OpenAI 설정 (RAG 기능용)
# ===================================
# OpenAI API 키가 필요한 경우에만 설정
OPENAI_API_KEY=
OPENAI_MODEL=gpt-3.5-turbo
OPENAI_EMBEDDING_MODEL=text-embedding-ada-002

# ===================================
# 벡터 검색 설정
# ===================================
VECTOR_DIMENSION=1536
VECTOR_SIMILARITY_THRESHOLD=0.8
VECTOR_SEARCH_LIMIT=10

# ===================================
# 파일 업로드 설정
# ===================================
UPLOADS_BASE_DIR=front/public/uploads
METADATA_SUBDIR=metadata
THUMBNAIL_SUBDIR=thumbnails
VIDEO_SUBDIR=videos

# ===================================
# 클라우드 전용 설정 (App Runner에서만 사용)
# ===================================
# 이 설정들은 App Runner 환경변수로 주입됩니다
# DATABASE_URL=
# AWS_BEDROCK_REGION=ap-northeast-2