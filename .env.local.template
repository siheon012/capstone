# ===== 로컬 개발 환경 설정 템플릿 =====
# 이 파일을 복사하여 .env.local로 이름을 변경하고 사용하세요

# 환경 구분
APP_ENV=local
DOCKER_TARGET=development
NODE_ENV=development

# 프로젝트 설정
COMPOSE_PROJECT_NAME=capstone-local

# 포트 설정 (로컬 개발용)
BACKEND_PORT=8001
FRONTEND_PORT=3000
DB_PORT=5433
REDIS_PORT=6379
LOCALSTACK_PORT=4566

# Frontend 설정
NEXT_PUBLIC_API_URL=http://localhost:8001

# 데이터베이스 (로컬 PostgreSQL)
POSTGRES_DB=capstone_local
POSTGRES_USER=capstone_user
POSTGRES_PASSWORD=your_local_password_here

# JWT 설정 (로컬용 - 개발용으로만 사용)
SECRET_KEY=your-local-secret-key-for-development-only
DEBUG=true
ALLOWED_HOSTS=localhost,127.0.0.1,backend,0.0.0.0

# 데이터베이스 설정 (Django)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=capstone_local
DB_USER=capstone_user
DB_PASSWORD=your_local_password_here
DB_HOST=db
DB_PORT=5432

# AWS 설정 (LocalStack 사용 - 실제 AWS 아님)
USE_LOCALSTACK=true
AWS_ENDPOINT_URL=http://localhost:4566
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_DEFAULT_REGION=us-east-1

# S3 설정 (LocalStack)
AWS_S3_BUCKET_NAME=capstone-local-video-bucket
AWS_S3_REGION=us-east-1

# S3 업로드 설정
AWS_S3_PRESIGNED_URL_EXPIRE=900  # 15분 (초 단위)
AWS_S3_DOWNLOAD_URL_EXPIRE=3600  # 1시간 (초 단위)

# JWT 설정 (비디오 업로드용)
JWT_UPLOAD_TOKEN_EXPIRE=3600  # 1시간 (초 단위)
JWT_USER_TOKEN_EXPIRE=604800  # 7일 (초 단위)

# SQS 설정 (LocalStack)  
AWS_SQS_QUEUE_URL=http://localhost:4566/000000000000/capstone-video-analysis-queue

# 클라우드 서비스 (로컬에서는 비활성화)
USE_CLOUD_SERVICES=false
